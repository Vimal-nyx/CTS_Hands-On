import { Routes } from '@angular/router';
import { Home } from './pages/home/home';

// Hands-On 1: Basic route configuration for Home page
export const routes: Routes = [
  { path: '', component: Home }
];
